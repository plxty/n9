diff --git a/dev-lang/rust-bin/rust-bin-1.97.1.ebuild b/dev-lang/rust-bin/rust-bin-1.97.1.ebuild
index 6a2fe3faac6e..6a77628d1aa9 100644
--- a/dev-lang/rust-bin/rust-bin-1.97.1.ebuild
+++ b/dev-lang/rust-bin/rust-bin-1.97.1.ebuild
@@ -7,6 +7,21 @@ LLVM_COMPAT=( 22 )
 LLVM_OPTIONAL="yes"
 
 inherit edo llvm-r2 multilib prefix rust-toolchain verify-sig multilib-minimal optfeature
+inherit dirty-deeds
+
+# filter to have only used platforms to reduce download size, @see rust-toolchain.eclass
+rust_all_arch_uris() {
+	local alt_basename="$1"
+	local rename_param="$2"
+
+	echo "
+	abi_x86_64? (
+		elibc_glibc? ( $(rust_arch_uri x86_64-unknown-linux-gnu  "${alt_basename}" "${rename_param}") )
+		elibc_musl?  ( $(rust_arch_uri x86_64-unknown-linux-musl "${alt_basename}" "${rename_param}") )
+	)
+	arm64-macos? ( $(rust_arch_uri aarch64-apple-darwin "${alt_basename}" "${rename_param}") )
+	"
+}
 
 if [[ ${PV} == *9999* ]]; then
 	# We need to fetch a tarball in src_unpack
@@ -24,7 +39,7 @@ else
 	SRC_URI="$(rust_all_arch_uris "rust-${PV}")
 		rust-src? ( ${RUST_TOOLCHAIN_BASEURL%/}/2026-07-16/rust-src-${PV}.tar.xz )
 	"
-	KEYWORDS="amd64 arm arm64 ~loong ~mips ppc ppc64 ~riscv ~s390 ~sparc x86"
+	KEYWORDS="amd64 ~arm64-macos"
 fi
 
 GENTOO_BIN_BASEURI="https://github.com/projg2/rust-bootstrap/releases/download/${PVR}" # omit trailing slash
@@ -32,27 +47,6 @@ GENTOO_BIN_BASEURI="https://github.com/projg2/rust-bootstrap/releases/download/$
 DESCRIPTION="Systems programming language from Mozilla"
 HOMEPAGE="https://www.rust-lang.org/"
 
-if [[ ${PV} != *9999* && ${PV} != *beta* ]] ; then
-	# Keep this separate to allow easy commenting out if not yet built
-	SRC_URI+=" sparc? ( ${GENTOO_BIN_BASEURI}/rust-${PVR}-sparc64-unknown-linux-gnu.tar.xz ) "
-	SRC_URI+=" mips? (
-		abi_mips_o32? (
-			big-endian?  ( ${GENTOO_BIN_BASEURI}/rust-${PVR}-mips-unknown-linux-gnu.tar.xz )
-			!big-endian? ( ${GENTOO_BIN_BASEURI}/rust-${PVR}-mipsel-unknown-linux-gnu.tar.xz )
-		)
-		abi_mips_n64? (
-			big-endian?  ( ${GENTOO_BIN_BASEURI}/rust-${PVR}-mips64-unknown-linux-gnuabi64.tar.xz )
-			!big-endian? ( ${GENTOO_BIN_BASEURI}/rust-${PVR}-mips64el-unknown-linux-gnuabi64.tar.xz )
-		)
-	)"
-	SRC_URI+=" riscv? (
-		elibc_musl? ( ${GENTOO_BIN_BASEURI}/rust-${PVR}-riscv64gc-unknown-linux-musl.tar.xz )
-	)"
-	SRC_URI+=" ppc64? ( elibc_musl? (
-		big-endian?  ( ${GENTOO_BIN_BASEURI}/rust-${PVR}-powerpc64-unknown-linux-musl.tar.xz )
-	) )"
-fi
-
 LICENSE="|| ( MIT Apache-2.0 ) BSD BSD-1 BSD-2 BSD-4"
 SLOT="${PV%%_*}" # Beta releases get to share the same SLOT as the eventual stable
 IUSE="big-endian +clippy cpu_flags_x86_sse2 doc prefix rust-analyzer rust-src +rustfmt"
@@ -62,7 +56,7 @@ RDEPEND="
 	>=app-eselect/eselect-rust-20190311
 	dev-libs/openssl
 	net-misc/curl
-	sys-apps/lsb-release
+	kernel_linux? ( sys-apps/lsb-release )
 	|| (
 		llvm-runtimes/libgcc
 		sys-devel/gcc:*
@@ -71,7 +65,7 @@ RDEPEND="
 	!dev-lang/rust-bin:stable
 "
 BDEPEND="
-	prefix? ( dev-util/patchelf )
+	kernel_linux? ( prefix? ( dev-util/patchelf ) )
 	verify-sig? ( sec-keys/openpgp-keys-rust )
 "
 [[ ${PV} == *9999* ]] && BDEPEND+=" net-misc/curl"
@@ -162,6 +156,12 @@ src_unpack() {
 	esac
 }
 
+if ! guse kernel_linux; then
+	patchelf() {
+		: # don't patchelf on darwin:
+	}
+fi
+
 patchelf_for_bin() {
 	local filetype=$(file -b ${1})
 	if [[ ${filetype} == *ELF*interpreter* ]]; then
@@ -211,8 +211,8 @@ rust_native_abi_install() {
 
 	if use prefix; then
 		local interpreter=$(patchelf --print-interpreter "${EPREFIX}"/bin/bash)
-		ebegin "Changing interpreter to ${interpreter} for Gentoo prefix at ${ED}/opt/rust-bin-${SLOT}/bin"
-		find "${ED}/opt/rust-bin-${SLOT}/bin" -type f -print0 | \
+		ebegin "Changing interpreter to ${interpreter} for Gentoo prefix at ${ED}/opt/rust-bin-${SLOT}/{bin,libexec}"
+		find "${ED}/opt/rust-bin-${SLOT}/"{bin,libexec} -type f -print0 | \
 			while IFS=  read -r -d '' filename; do
 				patchelf_for_bin ${filename} ${interpreter} \; || die
 			done
diff --git a/eclass/rust-toolchain.eclass b/eclass/rust-toolchain.eclass
index 41fccb7c4a07..8e1ce4b9c55a 100644
--- a/eclass/rust-toolchain.eclass
+++ b/eclass/rust-toolchain.eclass
@@ -54,6 +54,7 @@ rust_abi() {
 		sparc64*gnu)      echo sparc64-unknown-linux-gnu;;
 		x86_64*gnu)       echo x86_64-unknown-linux-gnu;;
 		x86_64*musl)      echo x86_64-unknown-linux-musl;;
+		arm64-apple-darwin*) echo aarch64-apple-darwin;;
 		*)                echo ${CTARGET};;
   esac
 }
@@ -118,5 +119,6 @@ rust_all_arch_uris()
 	riscv? ( elibc_glibc? ( $(rust_arch_uri riscv64gc-unknown-linux-gnu "${alt_basename}" "${rename_param}") ) )
 	s390?  ( elibc_glibc? ( $(rust_arch_uri s390x-unknown-linux-gnu     "${alt_basename}" "${rename_param}") ) )
 	loong? ( elibc_glibc? ( $(rust_arch_uri loongarch64-unknown-linux-gnu "${alt_basename}" "${rename_param}") ) )
+	arm64-macos? ( $(rust_arch_uri aarch64-apple-darwin "${alt_basename}" "${rename_param}") )
 	"
 }
